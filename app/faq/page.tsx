import { SimplePage } from "@/components/simple-page";

export default function FaqPage() {
  return (
    <SimplePage
      title="FAQ"
      description="This project captures startup failure patterns to help PMs and builders make better feature bets and avoid expensive blind spots."
    />
  );
}
